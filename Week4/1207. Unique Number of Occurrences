class Solution:
    def uniqueOccurrences(self, arr: List[int]) -> bool:
        list1 = []
        for i in set(arr):
            list1.append(arr.count(i))
        list2 = set(list1)
        return len(list1) == len(list2)

Runtime: 52 ms
Memory: 13.8 MB
